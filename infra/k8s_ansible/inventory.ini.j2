# inventory.ini.j2

[k8s_masters]
{{ VM_NAME }} ansible_host={{ IP }} ansible_user={{ USER }} ansible_ssh_private_key_file={{ lookup('env', 'HOME') + '/.ssh/' + ID_SSH }} ansible_python_interpreter=/usr/bin/python3
