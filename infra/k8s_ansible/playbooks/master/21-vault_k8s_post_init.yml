---
# playbooks/vault/vault_k8s_post_init.yml
# 🚀 Initialise et déverrouille le Vault déployé dans Kubernetes
# ⚙️  À exécuter depuis le bastion (kubectl configuré)
# 🔒  Sauvegarde root_token et unseal_key dans ~/.ansible/artifacts/master1/vault-k8s-init.json

- name: Initialiser et déverrouiller Vault Kubernetes
  hosts: bastion
  become: true
  vars:
    namespace: "vault"
    artifact_dir: "/root/.ansible/artifacts/master1"
    artifact_file: "{{ artifact_dir }}/vault-k8s-init.json"

  tasks:
    - name: Créer le dossier artefact s’il n’existe pas
      ansible.builtin.file:
        path: "{{ artifact_dir }}"
        state: directory
        mode: "0700"

    - name: Récupérer le nom du pod Vault
      ansible.builtin.command: >
        kubectl get pods -n {{ namespace }}
        -l app.kubernetes.io/name=vault
        -o jsonpath='{.items[0].metadata.name}'
      register: vault_pod
      changed_when: false

    - name: Vérifier si Vault est déjà initialisé
      ansible.builtin.command: >
        kubectl exec -n {{ namespace }} {{ vault_pod.stdout }} -- vault status
      register: vault_status
      changed_when: false
      failed_when: false

    - name: Interrompre si Vault déjà initialisé
      ansible.builtin.debug:
        msg: "✅ Vault Kubernetes déjà initialisé."
      when: vault_status.stdout is search("Initialized.*true")

    - name: Initialiser Vault Kubernetes
      ansible.builtin.command: >
        kubectl exec -n {{ namespace }} {{ vault_pod.stdout }} --
        vault operator init -key-shares=1 -key-threshold=1 -format=json
      register: vault_init
      when: vault_status.stdout is not search("Initialized.*true")

    - name: Sauvegarder les artefacts Vault (root_token + unseal_key)
      ansible.builtin.copy:
        content: "{{ vault_init.stdout }}"
        dest: "{{ artifact_file }}"
        mode: "0600"

    - name: Extraire root_token et unseal_key
      ansible.builtin.set_fact:
        vault_unseal_key: "{{ (vault_init.stdout | from_json).unseal_keys_b64[0] }}"
        vault_root_token: "{{ (vault_init.stdout | from_json).root_token }}"
      when: vault_status.stdout is not search("Initialized.*true")

    - name: Déverrouiller Vault
      ansible.builtin.command: >
        kubectl exec -n {{ namespace }} {{ vault_pod.stdout }} --
        vault operator unseal {{ vault_unseal_key }}
      when: vault_status.stdout is not search("Initialized.*true")

    - name: Vérifier l’état final du Vault
      ansible.builtin.command: >
        kubectl exec -n {{ namespace }} {{ vault_pod.stdout }} -- vault status
      register: vault_final_status
      changed_when: false

    - name: Afficher le statut final
      ansible.builtin.debug:
        msg: "{{ vault_final_status.stdout_lines }}"

    - name: Afficher les tokens tronqués (sécurité)
      ansible.builtin.debug:
        msg:
          - "Root token: {{ vault_root_token[:8] | default('N/A') }}..."
          - "Unseal key: {{ vault_unseal_key[:8] | default('N/A') }}..."
      when: vault_status.stdout is not search("Initialized.*true")

    - name: Rappel de sécurité
      ansible.builtin.debug:
        msg: >
          📤 Transfère immédiatement le root_token et la unseal_key
          dans Bitwarden (collection 'Vault Kubernetes Root / Unseal Keys'),
          puis supprime {{ artifact_file }} si nécessaire.
